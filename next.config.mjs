/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
         "api.microlink.io", 
        ], // Add the external hostname here
      },
    // output: 'export',
};

export default nextConfig;
